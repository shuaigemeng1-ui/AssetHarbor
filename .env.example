# --- 端口（容器内固定监听 8080，这里映射到宿主机） ---
PORT=8080

# --- 单文件上传大小上限（MB） ---
MAX_UPLOAD_SIZE_MB=10

# --- 图片 URL 短码长度（base62 字符） ---
SHORT_CODE_LENGTH=10

# --- 返回给用户的完整链接前缀，例如 https://img.example.com ---
# 留空则自动从请求的 Host 推断（适合直接 IP + 端口访问）
PUBLIC_URL=
