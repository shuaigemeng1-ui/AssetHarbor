# OSS Media 极简重构 Design QA

- 视觉真值：`C:\Users\user\.codex\generated_images\019ff8ff-73a6-7542-b933-b258ae43647d\exec-2f4d82a7-262d-44e2-a9c5-72619be94d23.png`
- 实现入口：`C:\Users\user\Desktop\oss\frontend\src\App.vue`
- 核心页面：`C:\Users\user\Desktop\oss\frontend\src\components\GalleryView.vue`
- 目标视口：1440 × 1024 CSS px，device scale factor 1
- 源图尺寸：1487 × 1058 px
- 实现截图：未采集；项目指令禁止擅自启动前端。
- 目标状态：个人图片库、图片已加载、选中一张图片、右侧展示详情。

## 已完成的静态对齐

- 三栏结构：64px 全局图标栏、232px 空间导航、图库主区与 360px 图片详情栏。
- 视觉语言：纯白/暖灰背景、1px 分隔、小圆角、蓝色主动作；已移除主要渐变、玻璃模糊、大阴影和装饰性 CSS 图形。
- 图库：四列紧凑卡片、4:3 真实缩略图、选中描边、搜索与上传同一工具栏。
- 详情：预览、显示名称、原文件名、大小、类型、短码、权限、链接，以及重命名、加入分组、权限切换和删除。
- 响应式：常规图库在窄视口使用右侧抽屉；团队嵌入图库使用更少列与同一详情抽屉。
- 上传：从常驻面板迁移到弹窗，保留多文件、命名、可见性、额度说明、并发队列、失败重试与移除。
- 图标：可见操作统一使用 Lucide 图标，未新增假图片或虚构业务字段。

## 功能与静态验证

- `npm test -- --run`：26 个测试文件、98 项测试通过。
- `npm run build`：Vite 生产构建成功。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 提示。
- 未启动前端或后端。

### 视频播放器修正

- 根据竖屏视频溢出截图，将播放器舞台设为固定约束容器。
- 视频使用绝对定位填满舞台，并通过 `object-fit: contain` 等比例居中缩放；横竖屏均不裁切、不撑破弹窗。
- `viewport-fit` 弹窗面板与内容区禁止固有尺寸溢出，短视口高度同步扣除 48px 遮罩边距。
- 新增 `VideoPlayerModal` 结构回归测试；浏览器几何尺寸仍需在获得预览授权后验证。

## 浏览器级验证状态

当前无法执行有效的同视口并排视觉比较。Product Design QA 要求把源图和浏览器渲染截图放在同一比较输入中；仅凭源图和代码不能确认精确间距、字体渲染、图片裁切、滚动区域、断点与浏览器控制台状态。

在获得启动/预览授权后需要：

1. 在 1440 × 1024 下采集“个人图片库 + 选中详情”截图，与源图并排比较。
2. 在 1080 × 900、860 × 900、390 × 844 下验证网格、上下文导航、详情抽屉和上传弹窗。
3. 验证搜索、上传、选图、复制、重命名、加入分组、权限切换、删除和加载更多。
4. 验证团队嵌入图库、管理员全站图库和私密图片签名预览。
5. 检查浏览器控制台，并修复所有 P0/P1/P2 视觉差异后更新本报告。

final result: blocked
